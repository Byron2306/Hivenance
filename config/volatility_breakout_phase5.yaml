strategy_id: hivenance_ember_phase5_shadow_flight
phase: 5
mode: public_shadow_only
enabled: true
execution_wired: false
private_exchange_access: false
credential_use: false
live_eligible: false
real_orders_submitted: 0
transmission_attempts: 0
human_approval_required: true
parameter_freeze_required: true
shadow:
  max_intents_per_cycle: 25
  reference_notional_usd: 10.0
  stop_distance_bps: 250.0
  entry_latency_ms: 250.0
  chase_timeout_sec: 30
  settlement_tolerance_sec: 900
  min_data_quality: 0.99
  max_spread_bps: 60.0
readiness:
  min_distinct_days: 30
  min_settled_shadow_intents: 100
  min_hypothetical_fill_ratio: 0.50
  max_cost_model_mae_bps: 20.0
  require_positive_mean_net: true
  zero_transmission_attempts: true
  zero_real_orders: true
  frozen_parameters_unchanged: true
  human_review_required: true
forbidden:
  - authenticated_exchange_calls
  - private_api_calls
  - live_order_submission
  - credential_use
  - automatic_strategy_promotion
  - automatic_allocation_increase
  - balance_mutation
  - automatic_recovery
  - leverage
  - dex_execution
